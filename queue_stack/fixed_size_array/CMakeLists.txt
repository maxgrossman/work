cmake_minimum_required(VERSION 3.10)
project(fixed_size_array_queue)
include_directories(include)
file(GLOB SOURCES "src/*.cpp")
set(CMAKE_CXX_FLAGS_DEBUG_INIT "-Wall")
add_executable(test ${SOURCES})
